# AnimeTrack 后续事项

> 更新于 2026-07-25。项目仅保留 Web 版，桌面实现已通过 `desktop-archive-2026-07` 标签归档。

## 近期任务

- 修复 AI 在 Bangumi 已返回候选时偶发不选择的问题：最多重试一次，并增加严格的本地高置信度评分兜底。
- 为 AI 候选选择补充精确标题、别名、季度、歧义候选和无匹配测试。
- 单独规划 Next.js 大版本升级，解决当前生产依赖审计中的高危问题；不要直接运行 `npm audit fix --force`。
- 为 JSON/SQL 导出、备份恢复和管理员 API 权限补充测试。

## 行为稳定后重构

- 拆分 `lib/anime.ts` 中的类型映射、查询、写入和观看历史事务。
- 拆分 `AnimePageClient.tsx` 中的筛选分页、表单、快速录入和数据修改状态。
- 拆分 `AdminPageClient.tsx` 中的动漫管理和观看历史管理标签页。
- 评估 `lib/ai.ts` 和元数据合并模块是否需要继续拆分，但避免为了文件行数而拆分。

## 产品方向

- 评估是否在现有标签、声优和观看统计基础上，实现从 Bangumi 获取库外作品的个性化推荐。
- 根据真实使用反馈继续调整移动端和响应式界面。
